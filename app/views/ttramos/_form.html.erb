<%= form_for(@ttramo) do |f| %>
  <% if @ttramo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ttramo.errors.count, "error") %> prohibited this ttramo from being saved:</h2>

      <ul>
      <% @ttramo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo %><br />
    <%= f.text_field :tipo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
